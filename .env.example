# Copy this file to .env.local and fill in real values. .env.local is
# gitignored; this file is not -- never put real secrets in this one.

# Generated by running `npx auth secret` (writes directly into .env.local).
AUTH_SECRET=

# Vercel dashboard -> your project -> Storage -> Postgres -> connection string.
DATABASE_URL=

# Google Cloud Console -> APIs & Services -> Credentials -> OAuth client ID
# (Web application type). Add http://localhost:3000/api/auth/callback/google
# (and your production URL's equivalent once deployed) as an Authorized
# redirect URI.
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# 32-byte key, base64-encoded, encrypts twelve_data_api_key/finnhub_api_key
# at rest in user_settings. Generate with: openssl rand -base64 32
# Losing this key makes existing encrypted keys permanently undecryptable
# (users would just re-enter them in Settings).
SETTINGS_ENCRYPTION_KEY=
